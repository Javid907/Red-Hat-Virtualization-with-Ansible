---
  - hosts: all
    become: yes
    tasks:
      - name: install the latest version packages
        yum:
          name: "{{ packages }}"
          state: latest
        vars:
          packages:
          - nano
          - bash-completion
          - wget
